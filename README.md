# 🚀 Crypto Exchange

Сучасна та інтуїтивна платформа для обміну криптовалют з веб-інтерфейсом на базі Flask та Docker.

---

## 📋 Зміст

- [Вимоги](#-вимоги)
````markdown
# 🚀 Crypto Exchange

Сучасна та інтуїтивна платформа для обміну криптовалют з веб-інтерфейсом на базі FastAPI/Flask (у проекті використовується `uvicorn` для запуску) та Docker.

---

## 📋 Зміст

- [Вимоги](#-вимоги)
- [Встановлення](#-встановлення)
- [Запуск проекту](#-запуск-проекту)
- [Швидкий старт (корисні команди)](#-швидкий-старт-корисні-команди)
- [Структура проекту](#-структура-проекту)
- [API Документація](#-api-документація)
- [Розробка](#-розробка)
- [Конфігурація](#-конфігурація)
- [Налагодження та логування](#-налагодження-та-логування)

---

## 💻 Вимоги

Перед початком впевніться, що у вас встановлено:

- **Docker** (версія 20.10+)
- **Docker Compose** (версія 1.29+)
- **Python 3.9+** (для локальної розробки)

### Установка Docker та Docker Compose

**На Windows:**
- Завантажте та встановіть [Docker Desktop](https://www.docker.com/products/docker-desktop)

**На macOS та Linux:**
```bash
# Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

---

## 📦 Встановлення

1. **Клонуйте репозиторій:**
```bash
git clone https://github.com/Kokos96/Crypto_exchange.git
cd Crypto_exchange
```

2. **Встановіть залежності Python (опціонально для локальної розробки):**
```bash
pip install -r requirements.txt
```

---

## 🎯 Запуск проекту

Нижче — декілька способів запуску: Docker Compose (рекомендовано), ручне створення образу/контейнера або локальний запуск для розробки.

### 🐳 Рекомендований — Docker Compose

Docker Compose підніме базу даних, Redis, веб-сервіс, nginx (load balancer) та worker.

```powershell
# В корені проекту (Windows PowerShell)
docker-compose up -d --build

# Перевірити статус сервісів
docker-compose ps

# Переглянути логи в реальному часі для веб-сервісу
docker-compose logs -f web

# Зупинити та видалити контейнери і volumes (для чистого старту)
docker-compose down -v
```

Після запуску через Compose балансувальник буде доступний на `http://localhost` (порт 80). Веб-сервіс в контейнері слухає порт `8000` внутрішньо.

### 🐋 Ручне побудова та запуск Docker image

Якщо хочете зібрати образ вручну та запустити контейнер окремо:

```powershell
# Побудувати образ
docker build -t crypto_web .

# Запустити контейнер (публікуємо порт 8000 на хості)
docker run -d --name crypto_web -p 8000:8000 \
	-e DATABASE_URL="mysql+pymysql://root:root@localhost:3306/crypto_exchange" \
	-e REDIS_HOST=redis \
	crypto_web

# Перевірити логи
docker logs -f crypto_web
```

> Примітка: при запуску вручну контейнерам, що вимагають зовнішніх сервісів (MySQL, Redis), потрібно або підняти їх окремо (через Docker Compose) або змінити `DATABASE_URL`/`REDIS_HOST` на доступні хости/контейнери.

### 🧩 Запуск окремих сервісів через Compose

Можна стартувати лише певні сервіси, наприклад тільки базу та веб:

```powershell
docker-compose up -d db redis web

# Запуск тільки worker
docker-compose up -d worker
```

### 🧑‍💻 Локальний запуск для розробки (venv + uvicorn)

```powershell
# PowerShell (створення віртуального середовища)
python -m venv venv
.\venv\Scripts\Activate.ps1   # PowerShell
# або для cmd: venv\Scripts\activate

pip install -r requirements.txt

# Запуск dev-сервера з автоперезавантаженням
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервіс буде доступний на `http://localhost:8000` (зауважте: при запуску через Compose nginx зв'язує користувача із портом 80).

### 🧰 Запуск воркера, load-tester та autoscaler

- Запуск worker локально (окремий термінал):
```powershell
python worker.py
```

- Запуск load-тестера локально (налаштуйте `TARGET_URL` в `load_tester.py` або через змінну середовища):
```powershell
python load_tester.py
```

- Запуск autoscaler (скрипт виконує docker-compose scale на основі CPU):
```powershell
python autoscaler.py
```

> Примітка: `autoscaler.py` спілкується з Docker CLI — переконайтесь, що у вас є права на виконання `docker` з поточного оточення.

---

## 🛠️ Швидкий старт (корисні команди)

- Переглянути активні контейнери:
```powershell
docker ps
```

- Переглянути ідентифікатор контейнера для сервісу Compose:
```powershell
docker-compose ps -q web
```

- Переглянути логи конкретного контейнера:
```powershell
docker logs -f <container_id_or_name>
```

- Виконати шелл всередині контейнера (якщо потрібно):
```powershell
docker-compose exec web sh
```

---

## 🏗️ Структура проекту

```
Crypto_exchange/
├── app/                          # Основний пакет застосунку
│   ├── __init__.py              # Ініціалізація застосунку
│   ├── main.py                  # Головна логіка застосунку
│   ├── database.py              # Конфігурація бази даних
│   ├── models.py                # ORM моделі
│   ├── routers.py               # Маршрути
│   ├── templates/               # HTML шаблони
│   │   ├── index.html
│   │   └── login.html
│   └── __pycache__/
├── worker.py                     # Фоновий воркер
├── autoscaler.py                 # Простий autoscaler на основі docker-compose
├── load_tester.py                # Скрипт для локального навантажувального тестування
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

---

## 🔌 API Документація

### Основні ендпоінти

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| `GET` | `/` | Головна сторінка |
| `GET` | `/login` | Сторінка входу |
| `POST` | `/api/exchange` | Обміняти криптовалюту |
| `GET` | `/api/rates` | Отримати поточні курси |

---

## 🔧 Розробка

### Локальний запуск (без Docker)

1. **Створіть віртуальне середовище:**
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1  # PowerShell
# або для Bash: source venv/bin/activate
```

2. **Встановіть залежності:**
```powershell
pip install -r requirements.txt
```

3. **Запустіть застосунок:**
```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

---

## 📝 Конфігурація

Приклад `.env.example` (створіть `.env` на проді/локалі за потреби):

```
DATABASE_URL=mysql+pymysql://root:root@db:3306/crypto_exchange
REDIS_HOST=redis
MYSQL_ROOT_PASSWORD=root
```

Файли конфігурації/змінні оточення:
- `app/database.py` — конфігурація підключення до БД
- `app/main.py` — старт застосунку, middleware
- `docker-compose.yml` — склад сервісів для продакшен/локального запуску

---

## 🧭 Налагодження та логування

- Перевірка статусу контейнерів:
```powershell
docker-compose ps
docker ps
```
- Перегляд логів:
```powershell
docker-compose logs -f web
docker-compose logs -f worker
```
- Якщо потрібне створення таблиць (SQLAlchemy без Alembic):
```powershell
python -c "from app import models, database; db=database.SessionLocal(); models.Base.metadata.create_all(bind=db.bind); db.close()"
```

---

## 🤝 Розробники

Проект розроблений як демонстраційний обмінник криптовалют та приклад мікросервісної архітектури з використанням Docker.

---

## 📄 Ліцензія

Цей проект розповсюджується під ліцензією MIT. Див. деталі у файлі `LICENSE`.

---

**Приємної роботи з проєктом!**

````
