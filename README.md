# 🚀 Crypto Exchange

Сучасна та інтуїтивна платформа для обміну криптовалют з веб-інтерфейсом на базі Flask та Docker.

---

## 📋 Зміст

- [Вимоги](#-вимоги)
- [Встановлення](#-встановлення)
- [Запуск проекту](#-запуск-проекту)
- [Структура проекту](#-структура-проекту)
- [API Документація](#-api-документація)
- [Розробка](#-розробка)

---

## 💻 Вимоги

Перед початком впевніться, що у вас встановлено:

- **Docker** (версія 20.10+)
- **Docker Compose** (версія 1.29+)
- **Python 3.9+** (для локальної розробки)

### Установка Docker та Docker Compose

**На Windows:**
- Завантажте та встановіть [Docker Desktop](https://www.docker.com/products/docker-desktop)

**На macOS та Linux:**
```bash
# Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

---

## 📦 Встановлення

1. **Клонуйте репозиторій:**
```bash
git clone https://github.com/Kokos96/Crypto_exchange.git
cd Crypto_exchange
```

2. **Встановіть залежності Python (опціонально для локальної розробки):**
```bash
pip install -r requirements.txt
```

---

## 🎯 Запуск проекту

### 🐳 Запуск з Docker Compose (рекомендується)

```bash
# Запустити контейнери у фоновому режимі
docker-compose up -d --build

# Перевірити статус контейнерів
docker-compose ps

# Переглянути логи
docker logs crypto_web
```

**Платформа буде доступна за адресою:** `http://localhost:5000`

### 🛑 Зупинення контейнерів

```bash
docker-compose down
```

### 🔄 Перезапуск контейнерів

```bash
docker-compose restart
```

### 📊 Переглядання логів

```bash
# Логи вебзастосунку
docker logs crypto_web

# Логи воркера
docker logs crypto_worker

# Логи в режимі реального часу
docker logs -f crypto_web
```

---

## 🏗️ Структура проекту

```
Crypto_exchange/
├── app/                          # Основний пакет застосунку
│   ├── __init__.py              # Ініціалізація Flask застосунку
│   ├── main.py                  # Головна логіка застосунку
│   ├── database.py              # Конфігурація бази даних
│   ├── models.py                # ORM моделі
│   ├── routers.py               # Маршрути (routes)
│   ├── templates/               # HTML шаблони
│   │   ├── index.html           # Головна сторінка
│   │   └── login.html           # Сторінка входу
│   └── __pycache__/             # Кешована інформація Python
├── worker.py                     # Асинхронний воркер для фонових завдань
├── Dockerfile                    # Docker образ для застосунку
├── docker-compose.yml            # Конфіг для Docker Compose
├── requirements.txt              # Python залежності
└── README.md                     # Цей файл
```

---

## 🔌 API Документація

### Основні ендпоінти

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| `GET` | `/` | Головна сторінка |
| `GET` | `/login` | Сторінка входу |
| `POST` | `/api/exchange` | Обмінити криптовалюту |
| `GET` | `/api/rates` | Отримати поточні курси |

---

## 🔧 Розробка

### Локальний запуск (без Docker)

1. **Створіть віртуальне середовище:**
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

2. **Встановіть залежності:**
```bash
pip install -r requirements.txt
```

3. **Запустіть застосунок:**
```bash
python app/main.py
```

Застосунок буде доступний за адресою `http://localhost:5000`

### 🐛 Отримання логів для налагодження

```bash
# Якщо використовуєте Docker
docker logs crypto_web

# Перевірте, чи контейнер запущений
docker ps
```

---

## 📝 Конфігурація

Всі налаштування знаходяться у файлах:
- `app/database.py` - Конфіг бази даних
- `app/main.py` - Конфіг Flask застосунку
- `docker-compose.yml` - Docker налаштування

---

## 🤝 Розробники

Проект розроблений як криптовалютний обмінник з метою демонстрації сучасних веб-технологій.

---

## 📄 Ліцензія

Цей проект розповсюджується під ліцензією MIT. Див. деталі у файлі LICENSE.

---

**Приємної розробки! 🎉**
