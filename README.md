# Crypto Exchange 🚀

Децентралізована платформа для торгівлі криптовалютами з простим та зручним інтерфейсом.

---

## 📋 Описання

**Crypto Exchange** — це веб-додаток, який дозволяє користувачам:
- 💰 Купувати та продавати криптовалюти
- 💳 Управляти цифровим гаманцем
- 📊 Переглядати актуальні курси обміну
- 🔐 Безпечно зберігати свої активи

---

## 🏗️ Структура проекту

```
crypto_exchange/
├── app/
│   ├── __init__.py           # Ініціалізація модуля
│   ├── main.py               # Точка входу, головний файл запуску
│   ├── database.py           # Налаштування підключення до БД
│   ├── models.py             # Таблиці БД (гаманці, транзакції)
│   ├── routers.py            # Маршрути: /buy, /sell, /wallet
│   └── templates/
│       └── index.html        # HTML інтерфейс
├── docker-compose.yml        # Docker конфігурація
├── requirements.txt          # Залежності проекту
└── README.md                 # Цей файл
```

---

## 🛠️ Технології

- **Backend**: FastAPI (Python)
- **Database**: PostgreSQL / SQLite
- **Frontend**: HTML5 + CSS
- **Containerization**: Docker & Docker Compose

---

## 📦 Встановлення

### 1️⃣ Клонування репозиторію
```bash
git clone https://github.com/Kokos96/Crypto_exchange.git
cd Crypto_exchange
```

### 2️⃣ Встановлення залежностей
```bash
pip install -r requirements.txt
```

### 3️⃣ Запуск за допомогою Docker
```bash
docker-compose up --build
```

### 4️⃣ Запуск локально (без Docker)
```bash
uvicorn app.main:app --reload
```

---

## 🚀 Використання

Після запуску відкрийте браузер:
```
http://localhost:8000
```

### Доступні маршрути:
- **GET** `/` — Головна сторінка
- **POST** `/buy` — Купити криптовалюту
- **POST** `/sell` — Продати криптовалюту
- **GET** `/wallet` — Переглянути гаманець

---

## ⚡ Швидкий старт (для нетерпеливих)

### За допомогою Docker (найпростіше)
```bash
# 1. Перейдіть у папку проекту
cd Crypto_exchange

# 2. Збудуйте і запустіть контейнери
docker-compose up --build

# 3. Залізте в браузер
# Відкрийте: http://localhost:8000
```

### За допомогою Python (локально)
```bash
# 1. Переконайтесь що встановлений Python 3.8+
python --version

# 2. Створіть віртуальне оточення (опційно, але рекомендується)
python -m venv venv

# Активуйте його:
# На Windows:
venv\Scripts\activate
# На Linux/Mac:
source venv/bin/activate

# 3. Встановіть залежності
pip install -r requirements.txt

# 4. Запустіть FastAPI сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 5. Відкрийте браузер
# http://localhost:8000
```

### Перевірка роботи
```bash
# Чи запущений сервер? Спробуйте в іншому терміналі:
curl http://localhost:8000

# Чи доступна документація API?
http://localhost:8000/docs
```

---

## 📝 Файли проекту

| Файл | Призначення |
|------|-----------|
| `main.py` | FastAPI додаток і точка входу |
| `database.py` | Підключення до БД та сесії |
| `models.py` | SQLAlchemy моделі (User, Wallet, Transaction) |
| `routers.py` | API маршрути для купівлі/продажу |
| `index.html` | Веб-інтерфейс користувача |

---

## 🔧 Налаштування

Змініть змінні оточення у `.env` (якщо потрібно):
```
DATABASE_URL=postgresql://user:password@localhost/crypto_exchange
SECRET_KEY=your_secret_key
```

---

## 📊 Приклад БД

### Таблиця: Users
- `id` — ID користувача
- `username` — Ім'я користувача
- `email` — Email
- `password_hash` — Хеш пароля

### Таблиця: Wallets
- `id` — ID гаманця
- `user_id` — ID власника
- `balance` — Баланс

### Таблиця: Transactions
- `id` — ID транзакції
- `user_id` — ID користувача
- `type` — Тип (BUY/SELL)
- `amount` — Сума
- `timestamp` — Час виконання

---

## 🤝 Контрибьютинг

Приватний проект. Для змін зв'яжіться з автором.

---

## 📄 Ліцензія

MIT License — вільний для використання.

---

## 👨‍💻 Автор

**Kokos96** — розробник проекту

---

## 📞 Контакти

- GitHub: [Kokos96](https://github.com/Kokos96)
- Email: lev68819@gmail.com.com

---

**Останнє оновлення**: 11.12.2025
